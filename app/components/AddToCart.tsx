'use client';
import React from "react";

const AddToCart = () => {
  return <button className="btn btn-primary" onClick={() => console.log("add")}>Add to Cart</button>;
};

export default AddToCart;
